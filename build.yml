image: node:18-alpine
commands:
  - echo 'Starting build...'
  - echo 'Running tests...'
  - npm test # If you don't have tests configured, this will naturally fail!
  - exit 1 # Or just force a failure with this command
